export enum TooltipPosition {
  ABOVE = 'above',
  BELOW = 'below',
  LEFT = 'left',
  RIGHT = 'right',
  DEFAULT = 'above',
  TOPRIGHT = 'topright',
  DYNAMIC = 'dynamic', //used for ex: canvas where tooltip needs constant updates
}
